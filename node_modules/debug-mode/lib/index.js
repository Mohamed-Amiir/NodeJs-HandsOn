"use strict";

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var inspector = require('inspector');

/**
 * debugMode
 * A boolean: `true` if the debug mode is enabled, `false` otherwise.
 *
 * @name debugMode
 * @function
 */

var isDebugging = (typeof v8debug === "undefined" ? "undefined" : _typeof(v8debug)) === "object" || /--debug|--inspect/.test(process.execArgv.join(" ")) || Boolean(inspector.url());

module.exports = isDebugging;